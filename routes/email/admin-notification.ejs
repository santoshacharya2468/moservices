<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto+Condensed&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Roboto Condensed", sans-serif;
        font-weight: 500;
        line-height: 30px;
      }
      h1 {
        color: #0a007f;
      }
      div {
        color: #0a007f;
        font-size: 16px;
        letter-spacing: 1px;
        font-weight: 500;
      }
      .hero {
        color: red;
        font-weight: bold;
      }
      a {
        color: #0a007f;
      }
      .bold {
        font-weight: bold;
      }
      .button {
        border: none;
        color: white;
        padding: 16px 32px;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        font-size: 16px;
        margin: 4px 2px;
        transition-duration: 0.4s;
        cursor: pointer;
      }

      .button1 {
        background-color: white;
        color: red;
        border: 2px solid #0a007f;
      }

      .button1:hover {
        background-color: #0a007f;
        color: white;
      }
    </style>
  </head>
  <body>
    <div>
      Shop with following detail was registered on <%= result.registeredAt %>
    </div>

    <div>Email of service Provider: <%= result.owner.email %></div>
    <div>Business name:<%= result.businessName %></div>
    <div>
      Mobiles:<% result.mobiles.forEach(function(mobile){ %>
      <span class="non-bold"><%= mobile %></span>
      <% }) %>
    </div>
    <div>
      Address:<% result.address.forEach(function(add){ %>
      <span class="non-bold"><%= add %></span>
      <% }) %>
    </div>
    <div>Category: <%= result.category.name %></div>
    <div>District: <%= result.district %></div>
    <div>
      Duration: 1 year
    </div>
    <div>Started-on: <%= result.registeredAt %></div>
  </body>
</html>
